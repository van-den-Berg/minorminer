# direnv configuration for this project
# This file automatically activates the project virtualenv `.venv310`
# if present. If not present, it falls back to direnv's `layout python3`
# helper which will create a `.direnv/` virtual environment on first use.

if [ -f .venv310/bin/activate ]; then
    # source the venv created by the user: python -m venv .venv310
    source .venv310/bin/activate
    echo "Activated local virtualenv .venv310"
else
    # fallback: use direnv's layout helper (creates .direnv/ env)
    layout python3
fi

# Avoid leaking sensitive env into the shell prompt
#export VIRTUAL_ENV_DISABLE_PROMPT=1
